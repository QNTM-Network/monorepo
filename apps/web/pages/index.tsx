interface Props {
  quants: any;
}

const Web = ({ quants }: Props) => {
  return (
    <div
      style={{ display: "flex", alignItems: "center", flexDirection: "column" }}
    ></div>
  );
};

export default Web;
